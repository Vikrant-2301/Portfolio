export const metadata = {
    title: "Web Development",
    description: "Web development projects by Vikrant Yadav.",
};

export default function WebDevLayout({ children }) {
    return <>{children}</>;
}
