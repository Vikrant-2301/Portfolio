export const metadata = {
    title: "Journal",
    description: "Thoughts, insights, and updates from Vikrant.",
};

export default function JournalLayout({ children }) {
    return <>{children}</>;
}
