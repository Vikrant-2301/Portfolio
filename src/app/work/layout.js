export const metadata = {
    title: "Work",
    description: "Selected architectural projects by Vikrant Yadav.",
};

export default function WorkLayout({ children }) {
    return <>{children}</>;
}
