export const metadata = {
    title: "About",
    description: "Learn more about Vikrant Yadav, an architecture student and web developer.",
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
